#ifndef QNET_H
#define QNET_H

#include <QSystemTrayIcon>
#include <QNetworkAccessManager>
/*#include <QAction>
#include <QMenu>
#include <QCache>

class qnet : public QSystemTrayIcon
{
    Q_OBJECT
public:
    explicit qnet();
private slots:
   void requestXml();
   void readXml(QNetworkReply *reaply);
   void readIcon(QNetworkReply *reaply);
   void setAirport(QAction *action);

private:
   QMenu menu;
   QNetworkAccessManager *networkXmlAccess;
   QNetworkAccessManager *networkIconAccess;
   QString airport;
   QCache<QUrl, QIcon> iconCache;
   int retryDelaySec;
};

#endif // QNET_H
*/
